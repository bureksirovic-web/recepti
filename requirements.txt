# Core dependencies for the orchestra project
# LLM client uses only stdlib (urllib) — no external dependencies required
# This file is kept minimal to avoid unnecessary dependencies

# If you need to add external libraries in the future, uncomment and modify:
# openai>=1.0.0  # For official OpenAI client (optional)
# requests>=2.28.0  # For alternative HTTP client (optional)

python-telegram-bot>=21.0.0
requests>=2.31.0
ruff>=0.4.0
duckduckgo-search>=6.0.0
lxml>=5.0.0
flask>=3.0.0
flask-cors>=4.0.0